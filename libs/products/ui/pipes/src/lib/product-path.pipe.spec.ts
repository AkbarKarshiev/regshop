import { ProductPathPipe } from './product-path.pipe';

describe('ProductPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductPathPipe();
    expect(pipe).toBeTruthy();
  });
});
