import { CartTotalPricePipe } from './cart-total-price.pipe';

describe('CartTotalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CartTotalPricePipe();
    expect(pipe).toBeTruthy();
  });
});
