import { CartTotalCountPipe } from './cart-total-count.pipe';

describe('CartTotalCountPipe', () => {
  it('create an instance', () => {
    const pipe = new CartTotalCountPipe();
    expect(pipe).toBeTruthy();
  });
});
